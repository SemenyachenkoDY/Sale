version: "3.9"

services:
  analytics:
    build:
      context: ./app
    container_name: supply-chain-analytics
    env_file: .env
    volumes:
      - ./data:/data:ro
      - ./output:/output
    restart: "no"
